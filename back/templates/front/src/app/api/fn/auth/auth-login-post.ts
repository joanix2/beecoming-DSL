/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { LoginInput } from '../../models/login-input';
import { TokenOutput } from '../../models/token-output';

export interface AuthLoginPost$Params {
      body?: LoginInput
}

export function authLoginPost(http: HttpClient, rootUrl: string, params?: AuthLoginPost$Params, context?: HttpContext): Observable<StrictHttpResponse<TokenOutput>> {
  const rb = new RequestBuilder(rootUrl, authLoginPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TokenOutput>;
    })
  );
}

authLoginPost.PATH = '/auth/login';
