/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OperatorOutput } from '../../models/operator-output';

export interface UsersOperatorsGet$Params {
  role?: string;
  date?: string;
  dateTo?: string;
}

export function usersOperatorsGet(http: HttpClient, rootUrl: string, params?: UsersOperatorsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OperatorOutput>>> {
  const rb = new RequestBuilder(rootUrl, usersOperatorsGet.PATH, 'get');
  if (params) {
    rb.query('role', params.role, {});
    rb.query('date', params.date, {});
    rb.query('dateTo', params.dateTo, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OperatorOutput>>;
    })
  );
}

usersOperatorsGet.PATH = '/users/operators';
