/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileUrlResponse } from '../../models/file-url-response';

export interface FilesSettingsFlaviconPost$Json$Params {
      body?: {
'file'?: Blob;
}
}

export function filesSettingsFlaviconPost$Json(http: HttpClient, rootUrl: string, params?: FilesSettingsFlaviconPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<FileUrlResponse>> {
  const rb = new RequestBuilder(rootUrl, filesSettingsFlaviconPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileUrlResponse>;
    })
  );
}

filesSettingsFlaviconPost$Json.PATH = '/files/settings/flavicon';
