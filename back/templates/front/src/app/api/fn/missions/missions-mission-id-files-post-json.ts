/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileInfoResponse } from '../../models/file-info-response';

export interface MissionsMissionIdFilesPost$Json$Params {
  missionId: string;
  forceReplace?: boolean;
      body?: {
'file'?: Blob;
}
}

export function missionsMissionIdFilesPost$Json(http: HttpClient, rootUrl: string, params: MissionsMissionIdFilesPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<FileInfoResponse>> {
  const rb = new RequestBuilder(rootUrl, missionsMissionIdFilesPost$Json.PATH, 'post');
  if (params) {
    rb.path('missionId', params.missionId, {});
    rb.query('forceReplace', params.forceReplace, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileInfoResponse>;
    })
  );
}

missionsMissionIdFilesPost$Json.PATH = '/missions/{missionId}/files';
