/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileInfoResponse } from '../../models/file-info-response';

export interface MissionsMissionIdFilesPost$FormData$Params {
  missionId: string;
  forceReplace?: boolean;
      body?: {
'file'?: Blob;
}
}

export function missionsMissionIdFilesPost$FormData(http: HttpClient, rootUrl: string, params: MissionsMissionIdFilesPost$FormData$Params, context?: HttpContext): Observable<StrictHttpResponse<FileInfoResponse>> {
  const rb = new RequestBuilder(rootUrl, missionsMissionIdFilesPost$FormData.PATH, 'post');
  if (params) {
    rb.path('missionId', params.missionId, {});
    rb.query('forceReplace', params.forceReplace, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileInfoResponse>;
    })
  );
}

missionsMissionIdFilesPost$FormData.PATH = '/missions/{missionId}/files';
