/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MissionOutput } from '../../models/mission-output';

export interface MissionsMissionsDetailedGet$Params {
  dateFrom?: string;
  dateTo?: string;
  includeFinished?: boolean;
}

export function missionsMissionsDetailedGet(http: HttpClient, rootUrl: string, params?: MissionsMissionsDetailedGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MissionOutput>>> {
  const rb = new RequestBuilder(rootUrl, missionsMissionsDetailedGet.PATH, 'get');
  if (params) {
    rb.query('dateFrom', params.dateFrom, {});
    rb.query('dateTo', params.dateTo, {});
    rb.query('includeFinished', params.includeFinished, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MissionOutput>>;
    })
  );
}

missionsMissionsDetailedGet.PATH = '/missions/missions-detailed';
