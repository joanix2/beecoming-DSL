/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MissionDocumentOutput } from '../../models/mission-document-output';

export interface MissionUploadDocumentsPost$FormData$Params {

/**
 * ID of the mission
 */
  missionId?: string;
      body?: {

/**
 * The files to upload
 */
'files'?: Array<Blob>;
}
}

export function missionUploadDocumentsPost$FormData(http: HttpClient, rootUrl: string, params?: MissionUploadDocumentsPost$FormData$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<MissionDocumentOutput>>> {
  const rb = new RequestBuilder(rootUrl, missionUploadDocumentsPost$FormData.PATH, 'post');
  if (params) {
    rb.query('missionId', params.missionId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<MissionDocumentOutput>>;
    })
  );
}

missionUploadDocumentsPost$FormData.PATH = '/mission/upload-documents';
