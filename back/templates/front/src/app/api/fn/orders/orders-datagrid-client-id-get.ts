/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrderListOutputListResponse } from '../../models/order-list-output-list-response';

export interface OrdersDatagridClientIdGet$Params {
  clientId: string;

/**
 * The max number of records.
 */
  '$top'?: Number;

/**
 * The number of records to skip.
 */
  '$skip'?: Number;

/**
 * A function that must evaluate to true for a record to be returned.
 */
  '$filter'?: String;

/**
 * Determines what values are used to order a collection of records.
 */
  '$orderby'?: String;

/**
 * A function that must evaluate to true for a record to be returned.
 */
  '$search'?: String;
}

export function ordersDatagridClientIdGet(http: HttpClient, rootUrl: string, params: OrdersDatagridClientIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<OrderListOutputListResponse>> {
  const rb = new RequestBuilder(rootUrl, ordersDatagridClientIdGet.PATH, 'get');
  if (params) {
    rb.path('clientId', params.clientId, {});
    rb.query('$top', params['$top'], {});
    rb.query('$skip', params['$skip'], {});
    rb.query('$filter', params['$filter'], {});
    rb.query('$orderby', params['$orderby'], {});
    rb.query('$search', params['$search'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrderListOutputListResponse>;
    })
  );
}

ordersDatagridClientIdGet.PATH = '/orders/datagrid/{clientId}';
