import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "fr.beecoming.opteeam.dev",
  appName: "opteeam",
  webDir: "dist/opteeam-front",
};

export default config;

